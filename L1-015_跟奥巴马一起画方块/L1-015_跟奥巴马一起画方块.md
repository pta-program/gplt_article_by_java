# L1-015 - 跟奥巴马一起画方块（15 分）

- **时间限制**: 400 ms
- **内存限制**: 65536 KB
- **代码长度限制**: 16 KB

---

## 题目描述


美国总统奥巴马不仅呼吁所有人都学习编程，甚至以身作则编写代码，成为美国历史上首位编写计算机代码的总统。2014年底，为庆祝“计算机科学教育周”正式启动，奥巴马编写了很简单的计算机代码：在屏幕上画一个正方形。现在你也跟他一起画吧！

### 输入格式:

输入在一行中给出正方形边长$$N$$（$$3\le N\le 21$$）和组成正方形边的某种字符`C`，间隔一个空格。

### 输出格式:

输出由给定字符`C`画出的正方形。但是注意到行间距比列间距大，所以为了让结果看上去更像正方形，我们输出的行数实际上是列数的50%（四舍五入取整）。

### 输入样例:
```in
10 a
```

### 输出样例:
```out
aaaaaaaaaa
aaaaaaaaaa
aaaaaaaaaa
aaaaaaaaaa
aaaaaaaaaa
```

## 示例

### 示例 1

**输入:**
```
10 a
```

**输出:**
```
aaaaaaaaaa
aaaaaaaaaa
aaaaaaaaaa
aaaaaaaaaa
aaaaaaaaaa
```

### 解题思路
列数 = N，行数 = N/2 四舍五入，即 (N+1)/2（整数运算等价于 Math.round(N/2.0)）。 为兼容 Java8，不使用 String.repeat，改为手动构造行字符串后循环输出。 时间复杂度 O(N^2)（输出规模），空间复杂度 O(N)。关键实现上，使用 StringBuilder 拼接输出提升效率。能够满足题目对时间与内存的限制。对于 L1 基础题，该方法以模拟与直接计算为主，逻辑清晰、边界处理简单，易于验证正确性。

### 代码流程说明
1. 启动程序，创建 Scanner 读取标准输入
2. 读取输入参数：n, c 等，用于后续计算
3. 读取边长 N 与字符 c，计算行数 rows=(N+1)/2（四舍五入），构造长度为 N 的单行字符串
4. 循环 rows 次输出该行，形成方块图形
5. 程序结束，关闭输入流并退出

### 代码实现
```java
import java.util.Scanner;

/**
 * L1-015 跟奥巴马一起画方块
 * 实现原理：列数 = N，行数 = N/2 四舍五入，即 (N+1)/2（整数运算等价于 Math.round(N/2.0)）。
 * 为兼容 Java8，不使用 String.repeat，改为手动构造行字符串后循环输出。
 * 时间复杂度 O(N^2)（输出规模），空间复杂度 O(N)。
 */
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        char c = scanner.next().charAt(0);
        int rows = (n + 1) / 2;
        // 手动构造一行，避免 Java11 的 String.repeat
        StringBuilder lineBuilder = new StringBuilder(n);
        for (int i = 0; i < n; i++) lineBuilder.append(c);
        String line = lineBuilder.toString();
        for (int i = 0; i < rows; i++) {
            System.out.println(line);
        }
    }
}
```

### 代码流程图
```mermaid
flowchart TD
    A[开始] --> B[读取 N 与字符 c]
    B --> C[计算 rows=(N+1)/2]
    C --> D[构造行字符串 line=n*c]
    D --> E{循环 rows 次}
    E --> F[输出 line]
    F --> E
    E --> G[结束]
```

### 解题流程图
```mermaid
flowchart TD
    A[理解题意：跟奥巴马一起画方块] --> B[选择算法：列数 = N，行数 = N/2 四舍五入，即 (N+1)/2]
    B --> C[确定数据结构与公式]
    C --> D[编码实现并处理边界]
    D --> E[测试样例验证]
    E --> F[格式化输出结果]
    F --> Z[结束]
```
